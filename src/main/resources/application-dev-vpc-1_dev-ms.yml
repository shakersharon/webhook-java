spring:
  application:
    name: webhook-java
    message: Welcome To webhook-java
server:
  port: 8080

management:
  server:
    port: 8081
  endpoints:
    web:
      exposure:
        include: "*"
    health.show-details: "always"
